# Telepath environment variables
# Copy to .env for local development only. Never commit .env.

# Required for MVP. Server-side only in /api/ai.ts.
ANTHROPIC_API_KEY=

# Optional hardening overrides for /api/ai.ts.
# Comma-separated origins allowed to call the edge proxy from browsers.
# ALLOWED_ORIGINS=http://localhost:5173,https://your-domain.vercel.app

# Comma-separated Anthropic models allowed by the edge proxy.
# ALLOWED_ANTHROPIC_MODELS=claude-3-5-sonnet-latest,claude-3-5-haiku-latest
