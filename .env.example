# Telepath — Environment Variables
# Copy this file to .env and fill in real values for local development.
# NEVER commit .env — only .env.example goes in the repo.

# Anthropic API key — used server-side only in /api/ai.ts
# Get yours at: https://console.anthropic.com
# This key must ONLY be set in Vercel environment variables for production.
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# (1.0 only — leave blank for MVP)
# Supabase project URL and anon key — safe to expose client-side
# NEXT_PUBLIC_SUPABASE_URL=
# NEXT_PUBLIC_SUPABASE_ANON_KEY=
